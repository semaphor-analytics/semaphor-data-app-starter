# Semaphor Query State Boundary

SDK-shaped loading, error, empty, partial, and stale-data state handling for
Semaphor Data App query results.

Use this component when a view receives a raw `useSemaphorQuery(...)` result and
needs consistent boundary behavior without hiding the underlying SDK payload.


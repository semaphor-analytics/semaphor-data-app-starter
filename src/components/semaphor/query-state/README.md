# Query State

Reusable loading, error, empty, and success states for Semaphor-backed Data App
views.

This component does not know about a specific query kind. Pass public SDK query
state into it and keep the query spec visible in the app source.


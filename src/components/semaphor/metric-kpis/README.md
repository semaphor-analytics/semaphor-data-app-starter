# Semaphor Metric KPIs

KPI card helpers for Semaphor metric query results.

The components render primary metric values, multi-measure metric maps, and
query-level comparison badges while preserving SDK-shaped loading, partial,
stale, empty, and error handling through `SemaphorQueryStateBoundary`.

